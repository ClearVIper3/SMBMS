<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.viper.dao.user.UserMapper">
    <select id="getLoginUser" parameterType="string" resultType="user">
        select * from smbms_user where userCode = #{userCode}
    </select>

    <select id="getUserCount" resultType="int">
        select count(1) as count from smbms_user u,smbms_role r
        <where>
            u.userRole = r.id
            <if test="userName != null and userName != ''">
                AND u.`userName` LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="userRole > 0">
                and u.userRole = #{userRole}
            </if>
        </where>
    </select>

    <select id="getUserList" resultType="user">
        select u.*,r.roleName as userRoleName from smbms_user u,smbms_role r
        <where>
            u.userRole = r.id
            <if test="userName != null and userName != ''">
                and u.userName LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="userRole > 0">
                and u.userRole = #{userRole}
            </if>
        </where>
        order by u.`creationDate` DESC limit #{startIndex},#{pageSize}
    </select>

    <select id="getUserById" parameterType="string" resultType="user">
        select u.*,r.roleName as userRoleName from smbms_user u,smbms_role r
        <where>
            u.id=#{id} and u.userRole = r.id
        </where>
    </select>
</mapper>